<div class="limit">
        {$limitForm->render('begin')}
        {$limitForm->render('hidden')}
        {$limitForm->render('js')}
        {$limitLabel} {=$limitForm['limit']->getSource()}
        {$limitForm->render('end')}
</div>
